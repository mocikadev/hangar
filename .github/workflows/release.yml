name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  verify:
    name: Verify release tag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check tag matches cli/gui Cargo.toml versions
        run: |
          TAG="${GITHUB_REF_NAME}"
          for CRATE in crates/cli crates/gui; do
            CARGO_VERSION="v$(grep '^version' $CRATE/Cargo.toml | head -1 | cut -d'"' -f2)"
            if [ "$TAG" != "$CARGO_VERSION" ]; then
              echo "error: tag '$TAG' does not match $CRATE/Cargo.toml version '$CARGO_VERSION'"
              exit 1
            fi
          done
          echo "OK: $TAG"

  build:
    name: Build · ${{ matrix.artifact }}
    needs: verify
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-latest
            use-cross: true
            artifact: linux-amd64
          - target: aarch64-unknown-linux-musl
            runner: ubuntu-latest
            use-cross: true
            artifact: linux-arm64
          - target: x86_64-apple-darwin
            runner: macos-latest
            use-cross: false
            artifact: macos-amd64
          - target: aarch64-apple-darwin
            runner: macos-latest
            use-cross: false
            artifact: macos-arm64
          - target: x86_64-pc-windows-msvc
            runner: windows-latest
            use-cross: false
            artifact: windows-x86_64

    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross
        if: matrix.use-cross
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Build (Unix)
        if: runner.os != 'Windows'
        run: |
          if [ "${{ matrix.use-cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }} &&
            cross build --release -p hangar-gui --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }} &&
            cargo build --release -p hangar-gui --target ${{ matrix.target }}
          fi

      - name: Build (Windows)
        if: runner.os == 'Windows'
        run: |
          cargo build --release --target ${{ matrix.target }}
          cargo build --release -p hangar-gui --target ${{ matrix.target }}

      - name: Stage artifact (Unix)
        if: runner.os != 'Windows'
        run: |
          cp target/${{ matrix.target }}/release/hangar hangar-${{ matrix.artifact }}
          cp target/${{ matrix.target }}/release/hangar-gui hangar-gui-${{ matrix.artifact }}

      - name: Stage artifact (Windows)
        if: runner.os == 'Windows'
        run: |
          copy target\${{ matrix.target }}\release\hangar.exe hangar-${{ matrix.artifact }}.exe
          copy target\${{ matrix.target }}\release\hangar-gui.exe hangar-gui-${{ matrix.artifact }}.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: hangar-${{ matrix.artifact }}
          path: hangar-${{ matrix.artifact }}*
          retention-days: 1

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create checksums
        working-directory: artifacts
        run: sha256sum hangar-* > SHA256SUMS.txt

      - uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/hangar-*
            artifacts/SHA256SUMS.txt
          generate_release_notes: true
