# v0.5.0 CLI/TUI 演进计划

> 状态：v0.5.0 已于 2026-09-22 发布；Linux/macOS 正式资产与 SHA-256 已回读验证
>
> 基线：v0.4.0 已发布；账号一致性、OAuth、配额和 TUI 已稳定。
>
> 目标：不扩展 GUI，在不改变凭据格式和 S11 写入不变量的前提下，补齐可脚本化一次性 CLI，并改善 TUI 配额交互。

## 范围边界

- 一次性 CLI 与 TUI 是 v0.5 新能力主入口。
- classic 保留现有交互和兼容 flags，不继续承载复杂新交互。
- GUI 保留在 workspace/CI，进入维护模式，不要求 v0.5 功能对等。
- 不改 `Account`/`AccountsFile` 持久化结构，不改 OAuth 常量，不新增 provider、守护进程、配额历史或钥匙串迁移。
- 初始实现阶段不包含版本、tag 与发布；2026-09-22 用户随后逐步授权版本升级、发布前审计以及创建并推送 `v0.5.0` tag，正式发布现已完成。

## Phase 0：执行基线

- [x] T01 更新 Spec：明确一次性 CLI、CLI/TUI 对等及 GUI 冻结
- [x] T02 更新执行协议、架构、测试策略和 AGENTS 导航
- [x] T03 建立 v0.5.0 执行契约与本任务清单

## Phase 1：一次性 CLI 基础

- [x] T04 引入严格参数解析：子命令、帮助、未知参数失败、兼容旧 flags
- [x] T05 建立 `args/commands/selector/output` 边界，不继续膨胀 `main.rs`
- [x] T06 实现 `list/current` human 与脱敏 JSON 输出
- [x] T07 实现完整 ID/唯一邮箱 selector；不存在或歧义必须失败
- [x] T08 实现 `switch/login/reauth/remove`，删除要求 `--yes`
- [x] T09 实现 `quota/doctor/harvest/update`，一次性命令不隐式升级
- [x] T10 建立稳定错误分类与退出码，stdout/stderr 分流

## Phase 2：TUI 改善

- [x] T11 修复“添加账号并自动切换”的错误提示
- [x] T12 启动时只查询当前或选中账号配额，不以全量网络任务锁住界面
- [x] T13 命令面板拆分“刷新当前配额”与“刷新全部配额”
- [x] T14 配额后台执行时仍允许移动、过滤和查看；逐账号记录失败
- [x] T15 补充 TUI reducer/render 测试和真实终端冒烟

## Phase 3：验证与收口

- [x] T16 参数、selector、JSON 脱敏和退出码单测
- [x] T17 隔离 HOME/CODEX_HOME 的二进制集成测试
- [x] T18 按序通过 `cargo fmt` → `cargo clippy -- -D warnings` → `cargo test`
- [x] T19 CLI release 构建、帮助/版本/只读命令冒烟
- [x] T20 README、架构、测试数量和任务状态同步

## Phase 4：发布准备

- [x] T21 CLI、GUI crate 与 Tauri bundle 版本统一升级到 0.5.0
- [x] T22 新增 v0.5.0 发布说明并同步用户文档
- [x] T23 按序重跑 `cargo fmt` → `cargo clippy -- -D warnings` → `cargo test`
- [x] T24 构建 release CLI、GUI 与 macOS DMG，核对版本和签名状态
- [x] T25 审计 Linux/macOS 资产上传模式与 SHA256SUMS 覆盖范围
- [x] T26 记录 Linux/macOS/Windows 平台证据缺口并给出发布结论

## Phase 5：正式发布

- [x] T27 创建并推送注释标签 `v0.5.0`，目标提交 `9c4505a`
- [x] T28 Release 工作流四平台构建及 GitHub Release 创建全绿
- [x] T29 下载 11 个线上资产，核对平台范围、文件集合和 10 项 SHA-256
- [x] T30 验证 macOS CLI/DMG 版本、架构、镜像完整性及签名状态并回写发布记录

## 命令契约

```text
hangar list [--json]
hangar current [--json]
hangar switch <完整ID|唯一邮箱> [--json]
hangar login [--json]
hangar reauth <完整ID|唯一邮箱> [--json]
hangar remove <完整ID|唯一邮箱> --yes [--json]
hangar quota [<完整ID|唯一邮箱>|--all] [--json]
hangar doctor [--json]
hangar harvest [--json]
hangar update [--json]
hangar tui
hangar classic
```

退出码：`0` 成功，`2` 参数错误，`3` selector/状态守卫失败，`4` 凭据需重新登录，`5` 网络或外部服务失败，`1` 其他内部错误。

## 完成定义

- 未知参数不再静默忽略；无子命令仍保持默认 TUI/非 TTY classic 行为。
- 所有 JSON 都由脱敏 DTO 显式构造，仓库测试证明不含 token 字段和值。
- selector 不以列表编号作为脚本契约，不存在或歧义时不产生写副作用。
- 删除没有 `--yes` 时失败且不等待 stdin。
- 一次性命令执行前不自动更新自身。
- 自动化门禁全绿，TUI 终端冒烟覆盖启动、移动、命令面板和退出。

## 验收记录

### 2026-09-22

- 用户批准按本计划直接执行；版本号、tag 与发布不在本次授权范围。
- `cargo fmt`、`cargo clippy -- -D warnings`、`cargo test` 按序通过；workspace 共 64 项测试（CLI 23、core 28、GUI 13）。
- `cargo build --release -p hangar` 通过；隔离沙箱验证 `--help`、`--version`、`list --json`。
- 真实 PTY 验证 TUI 启动、命令面板、当前/全部配额入口、Esc 返回与 `q` 正常退出；未使用真实凭据，不覆盖登录/刷新/在线配额成功链路。
- 提交 `acbd182` 后完成真实数据兼容冒烟：把现有账号库与官方配置复制到权限受限的隔离沙箱，`list/current` 正确识别 2 个账号与唯一当前账号，JSON 无 token 字段；联网前清空副本中的全部 refresh token，随后 `quota` 当前账号与 `quota --all` 均成功（2/2），因此该验证不可能触发 refresh token 轮换。真实 HOME/CODEX_HOME 未被测试命令写入，临时沙箱已移入废纸篓。
- 用户随后授权进入 v0.5.0 发布准备：允许升级版本号、整理发布说明和执行候选审计；本阶段不创建 tag、不推送、不发布。
- v0.5.0 本地候选：CLI、GUI crate 与 Tauri 配置统一为 0.5.0；`cargo fmt`、`cargo clippy -- -D warnings`、`cargo test` 按序通过，共 64 项测试。release CLI 与 GUI 构建通过，CLI `--version` 报告 0.5.0；隔离沙箱验证 JSON 列表、结构化参数错误和真实 PTY 的 TUI 启动/命令面板/退出。
- macOS arm64 DMG `hangar_0.5.0_aarch64.dmg` 构建并通过 `hdiutil verify`；bundle 版本 0.5.0、Bundle ID `dev.mocika.hangar`，`codesign --verify --deep --strict` 通过。签名为 ad-hoc、无 Team ID、未公证，`spctl` 按预期拒绝。CLI SHA-256 `806c49f553567bafe6d49513a6f4b38ab33010936a982da0768fd6b09681e150`，GUI 二进制 `dfe6322b3ad4f517bdd378fd57c4b03911eb64c99fbdc124ef82e1bb61653f41`，DMG `d8c82a4312f66f5116ce1faceb134cdae76ec8467f872a4e3bbb1b66fa85f9bf`。
- macOS GUI 真机冒烟：从候选 DMG 以隔离 HOME/CODEX_HOME 启动，系统识别 `dev.mocika.hangar` 正在运行，主窗口与账号/添加/切换/配额/自检/更新控件正常出现，随后正常退出并卸载镜像。
- 发布流水线静态审计：矩阵仅含 Linux amd64/arm64 与 macOS amd64/arm64；预计生成 4 个 CLI、2 个 DMG、2 个 DEB、1 个 RPM、1 个 AppImage，统一 checksum 步骤覆盖全部 10 个产品资产，上传规则包含 RPM 与 `SHA256SUMS.txt`。
- 平台证据边界：本机只验证 macOS arm64；Linux 两架构与 macOS Intel 的正式构建/资产校验必须由 tag Release 回读。Windows 只由普通 CI 验证且按范围不发布资产；本地候选结果本身不作为跨平台发布证据。
- 首轮远端 CI（run `35683039737`）：Linux、macOS 通过；Windows clippy 通过，但 5 个 CLI 二进制集成测试失败。根因是 Windows `dirs::home_dir()` 使用 Known Folder，忽略测试设置的 `HOME/USERPROFILE`，导致测试未读取临时账号库。
- 修复提交 `c14a5b9` 增加仅 debug/test 构建生效的 `HANGAR_TEST_HOME`，账号路径与 doctor 权限检查统一使用解析后的沙箱目录；release 构建不包含该测试入口。修复后本地 64 项测试与 release CLI/GUI 构建再次通过。
- 第二轮远端 CI（run `35683448646`）全绿：Ubuntu、macOS、Windows 均通过 workspace clippy、64 项测试与 GUI build。该结果证明三平台源码与宿主构建门禁通过；Linux musl 两架构、macOS 两架构正式安装包及最终校验文件仍只能由 `v0.5.0` tag 的 Release 工作流生成并回读。
- 用户明确授权后创建并推送注释标签 `v0.5.0`，标签指向 `9c4505a37b54f2dda2172d0d3849a545b05da2e6`。[Release run `35683949769`](https://github.com/mocikadev/hangar/actions/runs/35683949769) 全绿：标签校验、Linux amd64/arm64、macOS amd64/arm64 和最终 Release 创建全部成功。正式发布地址：<https://github.com/mocikadev/hangar/releases/tag/v0.5.0>。
- 线上 Release 为非草稿、非预发布，共 11 个资产：4 个 CLI、2 个 DMG、2 个 DEB、1 个 RPM、1 个 AppImage 和 `SHA256SUMS.txt`；没有 Windows 制品。下载后 10 个产品制品逐项通过 `SHA256SUMS.txt`，清单文件集合与下载文件集合完全一致。
- 官方 macOS arm64 CLI 在隔离 HOME/CODEX_HOME 下运行 `--version`，输出 `hangar 0.5.0`。两份官方 DMG 均通过 `hdiutil verify` 和 `codesign --verify --deep --strict`；bundle 版本均为 0.5.0、Bundle ID 均为 `dev.mocika.hangar`，内部二进制分别为 arm64/x86_64。签名仍为 ad-hoc、无 Team ID、未公证；macOS Intel 只做静态审计，未声称真机运行验证。
- Release 注释提示 `actions/upload-artifact@v4`、`actions/download-artifact@v4` 与 `softprops/action-gh-release@v2` 仍以 Node 20 为目标并由 GitHub 强制切到 Node 24；本次任务均成功，不影响 v0.5.0，但应在后续 CI 维护中升级对应 action major/version。
